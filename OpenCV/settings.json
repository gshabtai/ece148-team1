{
    "calibration_mode": true,
    "circle_detection": {
        "blur_kernel_size": 9,
        "_comment_blur_kernel_size": "Used for the kernel for the gausian convolution",
        "dp": 1.2,
        "_comment_dp": "(1.2)Inverse ratio of the accumulator resolution to the image resolution. For example, if dp=1 , the accumulator has the same resolution as the input image. If dp=2 , the accumulator has half as big width and height.",
        "minDist": 100,
        "_comment_minDist": "(100)Minimum distance between the centers of the detected circles. If the parameter is too small, multiple neighbor circles may be falsely detected in addition to a true one. If it is too large, some circles may be missed.",
        "param1": 100,
        "_comment_param1": "(100)First method-specific parameter. In case of HOUGH_GRADIENT , it is the higher threshold of the two passed to the Canny edge detector (the lower one is twice smaller).",
        "param2": 408,
        "_comment_param2": "(30)Second method-specific parameter. In case of HOUGH_GRADIENT , it is the accumulator threshold for the circle centers at the detection stage. The smaller it is, the more false circles may be detected. Circles, corresponding to the larger accumulator values, will be returned first.",
        "minRadius": 75,
        "_comment_minRadius": "(75)Minimum circle radius.",
        "maxRadius": 233,
        "_comment_maxRadius": "(400)Maximum circle radius. If <= 0, uses the maximum image dimension. If < 0, returns centers without finding the radius."
    },
    "color_detection": {
        "lower_hue1": 171,
        "lower_sat1": 151,
        "lower_val1": 132,
        "upper_hue1": 186,
        "upper_sat1": 231,
        "upper_val1": 179
    }
}